<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MATRIX-CLI OS — Hacker Terminal Console</title>
    <meta name="description" content="สไตล์การออกแบบ Hacker Terminal สไตล์แฮกเกอร์สายเขียว CRT Scanlines พรอมต์คำสั่ง">

    <!-- Google Fonts & Lucide Icons -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;600;700&family=JetBrains+Mono:wght@400;600;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        :root {
            --bg-term: #050505;
            --surface-term: #0a110a;
            --green-glow: #00ff66;
            --green-dim: #00aa44;
            --cyan-prompt: #38bdf8;
            --amber-warn: #fbbf24;

            --font-mono: 'Fira Code', 'JetBrains Mono', monospace;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: var(--bg-term);
            color: var(--green-glow);
            font-family: var(--font-mono);
            min-height: 100vh;
            line-height: 1.5;
            text-shadow: 0 0 4px rgba(0, 255, 102, 0.4);
        }

        /* Header */
        .header {
            position: sticky;
            top: 0;
            z-index: 100;
            background: #000;
            border-bottom: 1px solid var(--green-glow);
            padding: 14px 28px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
            color: var(--green-glow);
            font-weight: 700;
            font-size: 1.1rem;
        }

        .badge-style {
            background: rgba(0, 255, 102, 0.15);
            border: 1px solid var(--green-glow);
            color: var(--green-glow);
            font-size: 0.7rem;
            padding: 3px 10px;
        }

        .btn-hub {
            background: transparent;
            border: 1px solid var(--cyan-prompt);
            color: var(--cyan-prompt);
            padding: 6px 16px;
            text-decoration: none;
            font-size: 0.8rem;
            cursor: pointer;
        }

        .btn-hub:hover {
            background: var(--cyan-prompt);
            color: #000;
        }

        /* Container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 32px 24px 60px;
        }

        /* Terminal Window */
        .term-window {
            background: var(--surface-term);
            border: 1px solid var(--green-glow);
            box-shadow: 0 0 20px rgba(0, 255, 102, 0.25);
            border-radius: 6px;
            overflow: hidden;
        }

        .term-header {
            background: #000;
            border-bottom: 1px solid var(--green-glow);
            padding: 8px 16px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.8rem;
            color: var(--green-dim);
        }

        .term-body {
            padding: 24px;
            min-height: 380px;
            font-size: 0.85rem;
        }

        .prompt-line {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-top: 16px;
        }

        .prompt-input {
            background: transparent;
            border: none;
            color: var(--green-glow);
            font-family: var(--font-mono);
            font-size: 0.85rem;
            outline: none;
            flex: 1;
        }

        /* Footer */
        .footer {
            border-top: 1px solid var(--green-dim);
            padding: 20px;
            text-align: center;
            font-size: 0.8rem;
            color: var(--green-dim);
            margin-top: 60px;
        }
    </style>
</head>
<body>

    <!-- Header -->
    <header class="header">
        <a href="../pages/website_design.html" class="brand">
            >_ MATRIX-CLI v4.2
        </a>
        <span class="badge-style">Style #10: Hacker Terminal</span>
        <a href="../pages/website_design.html" class="btn-hub">
            <i data-lucide="grid"></i> 10 DESIGN VAULT
        </a>
    </header>

    <main class="container">
        
        <div class="term-window">
            <div class="term-header">
                <span>SYSTEM CONSOLE — root@agy-cli:~#</span>
                <span>STATUS: OPERATIONAL</span>
            </div>

            <div class="term-body" id="termOutput">
                <div>================================================================</div>
                <div>  __  __    _  _____ ____  _____  _  _       ____ _     ___ </div>
                <div> |  \/  |  / \|_   _|  _ \|_ _\ \/ /  | |     / ___| |   |_ _|</div>
                <div> | |\/| | / _ \ | | | |_) || | \  /   | |    | |   | |    | | </div>
                <div> | |  | |/ ___ \| | |  _ < | | /  \   | |___ | |___| |___ | | </div>
                <div> |_|  |_/_/   \_\_| |_| \_\___/_/\_\  |_____| \____|_____|___|</div>
                <div>================================================================</div>
                <div style="margin:12px 0;">Welcome to Matrix CLI OS. Type <span style="color:var(--cyan-prompt);">help</span> for available commands.</div>

                <div style="color:var(--green-dim);">[SYS] Telegram Bot daemon active (PID 12044)</div>
                <div style="color:var(--green-dim);">[SYS] Dashboard HTTP server listening on port 8080</div>
                <div style="color:var(--green-dim);">[SYS] Gold Micro EA engine synced with XAUUSD feed</div>

                <div id="dynamicHistory"></div>

                <div class="prompt-line">
                    <span style="color:var(--cyan-prompt);">root@agy-cli:~#</span>
                    <input type="text" id="cmdInput" class="prompt-input" placeholder="พิมพ์คำสั่ง เช่น help, status, bots, clear..." onkeydown="handleCmd(event)" autofocus>
                </div>
            </div>
        </div>

    </main>

    <footer class="footer">
        © 2026 MATRIX CLI OS — HACKER TERMINAL SHOWCASE
    </footer>

    <script>
        lucide.createIcons();

        function handleCmd(e) {
            if (e.key === 'Enter') {
                const input = document.getElementById('cmdInput');
                const val = input.value.trim().toLowerCase();
                const history = document.getElementById('dynamicHistory');

                if (!val) return;

                let output = '';
                if (val === 'help') {
                    output = `คำสั่งที่รองรับ: help, status, bots, fetch, clear`;
                } else if (val === 'status') {
                    output = `[STATUS REPORT]\n- CPU: 14%\n- RAM: 1.2GB / 8GB\n- Network: 1 Gbps Fiber\n- SSL: Active`;
                } else if (val === 'bots') {
                    output = `[BOT WORKERS]\n1. Market Monitor Bot (Running)\n2. Elliott Wave Analyzer (Idle)\n3. Shop Voucher Bot (Running)`;
                } else if (val === 'clear') {
                    history.innerHTML = '';
                    input.value = '';
                    return;
                } else {
                    output = `bash: command not found: ${val}. Type 'help' for options.`;
                }

                history.innerHTML += `<div style="margin-top:8px;"><span style="color:var(--cyan-prompt);">root@agy-cli:~#</span> ${input.value}</div>`;
                history.innerHTML += `<div style="color:var(--green-dim); white-space:pre-wrap;">${output}</div>`;

                input.value = '';
            }
        }
    </script>
</body>
</html>
